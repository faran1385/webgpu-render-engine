export enum GlobalBindPoints {
    PROJECTION_MATRIX,
    VIEW_MATRIX,
}


export enum PipelineShaderLocations {
    POSITION,
    UV,
    NORMAL,
    JOINTS,
    WEIGHTS,
    TANGENT,
    COLOR
}

export enum GeometryBindingPoint {
    MODEL_MATRIX,
    NORMAL_MATRIX,
    SKIN
}